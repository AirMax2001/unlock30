{
  "name": "unlock30",
  "version": "1.0.0",
  "description": "Il Gioco dei Trenta - Full Stack Application",
  "main": "backend/server.js",
  "license": "MIT",
  "scripts": {
    "start": "node start-server.js",
    "start-direct": "node backend/server.js",
    "build": "node copy-frontend.js --force-build",
    "postinstall": "npm run install-deps && npm run build",
    "install-deps": "cd backend && yarn install && cd ../frontend && yarn install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm run serve",
    "heroku-postbuild": "npm run install-deps && npm run build",
    "render-build": "npm run install-deps && npm run build"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "body-parser": "^1.20.2",
    "multer": "^2.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  },
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=8.0.0"
  }
}
